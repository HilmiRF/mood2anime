import React, { FC } from "react";

interface animePageProps {}

const animePage: FC<animePageProps> = ({}) => {
	return (
		<>
			<div className="text-center">Anime Page</div>
		</>
	);
};

export default animePage;
